<div>
  <h1> Edit Item: <%= @item.item_name %> </h1>
  <%= link_to 'Delete List?', list_item_path(@list), method: :delete, data: {confirm: 'Delete from list?'} %>

  <%= render(partial:'form') %>
  <br />
  <%=link_to '<< Back', lists_path(@list) %>
</div>
